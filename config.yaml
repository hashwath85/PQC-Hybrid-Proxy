network:
  server_host: "127.0.0.1"
  server_port: 8002
  gateway_host: "127.0.0.1"
  gateway_port: 8001

security:
  auth_token: "SUPER_SECRET_MESH_PASSPHRASE_12345!"
  session_ttl: 30.0
  rate_limit_window: 1.0

tls:
  enabled: true
  cert_file: "cert.pem"
  key_file: "key.pem"

forwarding:
  target_destination_url: "http://127.0.0.1:8080/api/receive"
